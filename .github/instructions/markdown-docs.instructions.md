---
description: 'Set markdown documentation quality expectations for repository docs and guides.'
applyTo: '**/*.md'
---

Use concise headings, predictable section structure, and repository-specific language.

Keep examples actionable and avoid speculative guidance.

When instructions already exist in `AGENTS.md`, reference them instead of duplicating full checklists.
